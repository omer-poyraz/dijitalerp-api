# Önce yanlış dosyayı silin
rm /Users/dev1/Desktop/omer/digitest/DijitalErpAPI/nginx/conf.d/default.conf

# Sonra doğru içerikle yeniden oluşturun
echo 'server {
    listen 80;
    server_name localhost;

    location /api/ {
        proxy_pass http://api:80/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

server {
    listen 443 ssl;
    server_name localhost;

    ssl_certificate /etc/nginx/ssl/nginx.crt;
    ssl_certificate_key /etc/nginx/ssl/nginx.key;

    location /api/ {
        proxy_pass http://api:80/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}' > /Users/dev1/Desktop/omer/digitest/DijitalErpAPI/nginx/conf.d/default.conf